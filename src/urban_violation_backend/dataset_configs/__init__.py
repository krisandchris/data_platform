"""Bundled fixture/example label config files.

These files are packaged only as reference fixtures and must not be auto-activated
at service startup. Active label configs are uploaded and managed through APIs.
"""
